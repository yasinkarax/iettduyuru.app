[build]
  # Tell Netlify to build from the frontend folder
  base = "frontend"
  # Build command
  command = "npm run build"
  # Where the built files are (FIXED - was frontend/frontend/dist)
  publish = "dist"

[build.environment]
  # Upgrade Node.js to version 20
  NODE_VERSION = "20"
